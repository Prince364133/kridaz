import { Router } from "express";

const router = Router();

// Placeholder for Owner-specific team management (e.g. venue-team partnerships)
// router.get("/partnerships", getTeamPartnerships);

export default router;
