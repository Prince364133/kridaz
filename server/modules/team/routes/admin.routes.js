import { Router } from "express";

const router = Router();

// Placeholder for Admin-specific team management (e.g. banning teams, global stats)
// router.get("/stats", getGlobalTeamStats);

export default router;
