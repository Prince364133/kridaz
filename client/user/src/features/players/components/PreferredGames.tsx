// This component has been integrated directly into PlayerProfileClient.tsx
// It remains here as a placeholder to avoid build errors if still referenced.
import React from 'react';

export const PreferredGames = ({ player }: { player: any }) => {
  return null;
};
